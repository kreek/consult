# Synced from eval/verifier/shared/reward.routing.toml. Do not edit in tests/.
[[reward]]
name = "reward"
aggregation = "weighted-mean"
weights = { no_file_writes = 0.55, judge = 0.45, skill_triggering = 0.0, non_interruption = 0.0 }
